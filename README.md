# Structure

## Hosts
All host specific configuration

### Structure

```bash
hosts/
└─ hp-victus-laptop/
 ├─ configuration.nix
 ├─ disk-configuration.nix
 ├─ facter.json
 └─ hardware-configuration.nix
```

Basically the hostname should be the name of the parent folder and all nix related configuration should go in the configuration file and so on


## Src
All common configuration across my apps

### Structure
tbd
